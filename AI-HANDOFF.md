# AI Handoff｜把 Xiazhouqi Warm 交给其他 AI

如果你把这个仓库交给另一个 AI，请让它先读取 `SKILL.md`，再按任务读取 `references/` 中对应文件。

推荐直接把下面这段一起发给 AI：

```text
请严格使用仓库中的 Xiazhouqi Warm｜下周七·暖杏 Skill 完成这次页面任务。

执行前：
1. 先完整读取 SKILL.md
2. 根据任务读取 references/ 中相关规范
3. 如果是修改旧 HTML，先保留并核对原始数据和功能
4. 先重构信息层级，再处理视觉
5. 最终输出前执行 anti-patterns-and-checklist.md

硬规则：
- Warm ≠ 全屏米黄色
- Warm ≠ 粉色可爱风
- Warm ≠ 大面积橙色渐变
- Warm ≠ 玻璃越多越高级
- 文案可以有一点文艺，但不能堆比喻、不能写成散文
- 不主动发明用户没有提供的信息
- 修改风格默认不修改数据
- 手机端必须无横向溢出
- 顶部分类导航默认不 sticky / fixed
- 普通内容不要全部做成同尺寸、同权重的大卡片
- 复制、密码、切换等真实功能必须优先可靠
- 单文件 HTML 优先，不依赖 CDN、外部字体或外部 JS
```

## 判断是否做对

最终页面应该让人感觉：

- 第一眼是温暖、干净、柔和
- 不是儿童感、少女粉、网红模板
- 有毛玻璃的空气感，但正文仍清晰
- 有淡橙色光感，但没有“橙色扑脸”
- 背景有变化但不抢正文
- 页面有生活感，但仍然像一个成熟的个人系统
- 手机端比桌面端 Demo 更重要
